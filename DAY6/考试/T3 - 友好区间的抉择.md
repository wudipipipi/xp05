# 友好区间的抉择

## 题目描述

小 C 维护了一个长度为 $n$ 的非负整数序列 $a_1, a_2, \dots, a_n$。

在这个序列中，小 C 对一种特殊的连续子段非常感兴趣，他称这种子段为“友好区间”。一个连续子段 $[L, R]$（$1 \le L \le R \le n$）被称为友好区间，当且仅当该子段内所有元素的**按位异或和**不小于它们的**算术和**。

形式化地说，子段 $[L, R]$ 是友好的当且仅当满足以下条件：


$$a_L \oplus a_{L+1} \oplus \dots \oplus a_R \ge (a_L + a_{L+1} + \dots + a_R)$$


其中 $\oplus$ 表示按位异或运算。

现在，小 C 提出了 $q$ 个询问。每个询问会给出两个整数 $l, r$（$1 \le l \le r \le n$），小 C 想知道，完全包含在区间 $[l, r]$ 内的**友好区间**共有多少个？即满足 $l \le L \le R \le r$ 且 $[L, R]$ 是友好区间的数对 $(L, R)$ 的数量。

## 输入格式

输入第一行包含两个正整数 $n$ 和 $q$，分别表示序列的长度和询问的次数。

输入第二行包含 $n$ 个非负整数，依次表示序列中的元素 $a_1, a_2, \dots, a_n$。

接下来 $q$ 行，每行包含两个正整数 $l, r$，表示一次询问的区间边界。

## 输出格式

输出共 $q$ 行，每行一个整数，依次对应每次询问的答案。

## 样例

### 样例输入

```text
4 3
3 0 2 4
1 4
1 3
2 4
```

### 样例输出

```text
8
4
6
```

### 样例解释

整个序列为 `[3, 0, 2, 4]`。

- 对于询问 `1 4`，共有 $10$ 个非空连续子段，其中 $[1,3]$ 和 $[1,4]$ 不满足条件，所以答案为 $8$。
- 对于询问 `1 3`，满足条件的子段为 $[1,1]$、$[1,2]$、$[2,2]$、$[3,3]$，共 $4$ 个。
- 对于询问 `2 4`，该范围内的 $6$ 个非空连续子段均满足条件。

## 数据范围与约定

本题采用测试点捆绑，同一行中的测试点作为一个测试点组计分。

对于所有测试数据，保证：

- $1 \le n, q \le 2 \times 10^5$；
- $0 \le a_i < 2^{20}$；
- $1 \le l \le r \le n$。

| 子任务 | 分值 | $n$ | $q$ | 特殊性质 |
| :--: | :--: | :--: | :--: | :--: |
| $1 \sim 4$ | $20$ | $100$ | $100$ | 无 |
| $5 \sim 8$ | $20$ | $2000$ | $2000$ | 无 |
| $9 \sim 12$ | $20$ | $2 \times 10^5$ | $2 \times 10^5$ | A |
| $13 \sim 16$ | $20$ | $2 \times 10^5$ | $2 \times 10^5$ | B |
| $17 \sim 20$ | $20$ | $2 \times 10^5$ | $2 \times 10^5$ | 无 |

特殊性质 A：保证序列中不含 $0$。

特殊性质 B：保证对于所有 $1 \leq i \leq n$，均有 $a_i \leq 1$。
